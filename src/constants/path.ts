enum CustomPath {
  HOME = "/",
  BRIGHTNESSTEST = "/brightness",
  SPEED_TEST_START = "/speed0",
  SPEED_TEST = "/speed",
  QUESTION = "/questions",
  RESULT = "/result",
}

export default CustomPath;
